# TMJ-Diagnosis プロジェクト におけるエージェントの役割とワークフロー

## 目的

このドキュメントは、LLM エージェントや人間の開発者が本プロジェクトに関わる際の役割分担、意思決定プロセス、作業手順を明文化し、チーム全体で共有することを目的とします。特に、本プロジェクトは段階的に仕様を拡張しながら進行しており、エージェントによる作業範囲・責務を明確にすることで、開発体験と成果物の品質を高めます。

## 共通原則

1. **仕様ファースト**  
   新機能は必ず `/docs` 配下の仕様書に落とし込んでから実装します。仕様には前提条件・操作方法・入出力を明記し、既存仕様との整合性を確認します。

2. **段階的なブランチ戦略**  
   - `development`: 開発の基盤となるブランチ。常に安定した状態を維持します。  
   - `feature/*`: 仕様書ごとに機能を追加する作業ブランチ。作業完了後は `development` にマージします。  
   - `docs-only/*`: ドキュメントだけを更新する場合に利用しても良いが、基本は機能ブランチ内で仕様書を更新します。

3. **Wasm & Frontend 並行整備**  
   Rust Wasm の API 追加時は、必ず TypeScript 側の型、Wasm ビルド成果物（`wasm/pkg`）を整合させた上でコミットします。

## 役割

| エージェント / メンバー | 主な担当 | 補足 |
| :--- | :--- | :--- |
| **仕様作成エージェント** | 指定された要望を仕様書として構造化する。過去仕様との整合・差分を整理する。 | 各フェーズの指針・要件・操作方法などを Markdown で作成。 |
| **Rust Wasm 開発エージェント** | `src/lib.rs` の拡張、`Cargo.toml` の依存管理、`npm run wasm:build` で成果物生成。 | JSON I/F と最適化（例: RDP）なども担当。 |
| **Next.js 開発エージェント** | `app/`・`components/` 等での UI 実装、Konva などのライブラリ統合。 | 仕様で定義された操作体系・編集機能を実装。 |
| **ビルド・リリースエージェント** | `npm run build`, `npm run wasm:build` などのビルド確認、Vercel との整合。 | エラー発生時は設定変更や依存調整を行う。 |
| **レビューエージェント** | コード・仕様の整合確認、抜け漏れチェック。 | 仕様の満たし方、操作性、ポイント数などを検証。 |

1 人のエージェントが複数役割を兼任しても問題ありません。重要なのは、仕様 → 実装 → 検証 → ドキュメント化のサイクルを守ることです。

## ワークフロー (例: 新仕様対応)

1. **要望ヒアリング**  
   ユーザー（人間）が新しい機能要望や修正点を提示します。

2. **仕様作成**  
   仕様作成エージェントが `/docs` に新しい仕様書 (例: `docs/tmj_diagnosis_feature_xyz_spec.md`) を作成。

3. **ブランチ運用**  
   `development` から新しい `feature/...` ブランチを切り、仕様を基に実装。必要に応じて Wasm & Frontend を同時更新。

4. **ビルド & テスト**  
   `npm run wasm:build` `npm run build` を実行し、エラーが出ないことを確認。手動確認が必要な UI は指示された操作手順で検証します。

5. **コミット & レビュー**  
   コミットメッセージは「仕様 → 実装内容」が分かるように記述。コードレビューでは仕様を満たしているかを重点チェックします。

6. **マージ & リリース**  
   `feature` ブランチを `development` にマージし、必要に応じて Vercel などでのビルド・デプロイを実行します。

## 注意事項

- 仕様書内の表やリストの整形が崩れた場合は、差分を確認しながら必ず修正すること。
- サンプルデータの場所（例: `./sample/`）は仕様書・README 等でも参照できるよう明記する。
- 操作ショートカットやポイント数など、ユーザー体験に直結する情報は仕様書にも UI にも重複して提示する。
- コミットは「目的単位」でまとめ、過度な肥大化や細分化を避ける。コミットメッセージは以下を推奨する。
  - **形式**: `<type>: <short description>`（例: `feat: add dual joint vector editor`）
  - **type の例**: `feat`, `fix`, `docs`, `refactor`, `chore`
  - **本文**: 仕様との関連や主要変更点を 1～3 行で補足（必要に応じて）
  - **粒度**: 仕様書・Rust・Frontend など大きなテーマごとに 1～数コミット。ビルド成果物（`wasm/pkg`）生成は同じコミットに含める。

## 関連ドキュメント

- `docs/tmj_diagnosis_prototype_spec.md` … プロジェクト初期仕様
- `docs/tmj_diagnosis_feature_segmentation_spec.md` … 単一ビューのセグメンテーション仕様
- `docs/tmj_diagnosis_feature_dual_joint_spec.md` … 両側顎関節編集 UI 仕様

今後、仕様や役割に変更がある場合は本ドキュメントを更新し、常に最新の開発方針を共有してください。
